{
  "installCommand": "npm install && cd contracts && npm install && cd ../backend && npm install && cd ../frontend && npm install",
  "buildCommand": "cd contracts && npx hardhat compile && cd ../backend && node src/seed.js && cd ../frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "backend/src/**,backend/package.json,backend/node_modules/**,backend/parcels.db,contracts/artifacts/contracts/CovenantRegistry.sol/CovenantRegistry.json,node_modules/better-sqlite3/**"
    }
  }
}
